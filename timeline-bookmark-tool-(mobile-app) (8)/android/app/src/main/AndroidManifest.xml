<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bbookmark">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- 
      다른 앱 위에 표시되는 플로팅 버튼을 위해 SYSTEM_ALERT_WINDOW 권한을 요청합니다.
      이 권한은 사용자가 직접 설정에서 활성화해야 합니다.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    
    <!-- 
      최상위 활성 앱 패키지 이름을 가져오기 위해 PACKAGE_USAGE_STATS 권한을 요청합니다.
      이 권한 또한 사용자가 직접 접근 허용 설정을 해야 합니다.
    -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />


    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- 플로팅 위젯을 관리하는 서비스(Service)를 등록합니다. -->
      <service android:name="com.bbookmark.FloatingWidgetService" />

    </application>
</manifest>
